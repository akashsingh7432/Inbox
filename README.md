# 📧 Personal Mail Manager

A modern, full-stack mail management application built with **Vite**, **TypeScript**, and **Node.js**.

## 🚀 Features
- **Fast UI:** Optimized frontend using Vite and React.
- **Type Safety:** Built entirely with TypeScript for robust development.
- **Backend Integration:** Custom server setup with `server.ts`.
- **Local Storage:** Integrated database (`mail.db`) for managing email metadata.

## 📂 Project Structure
- `src/`: Frontend components and logic (`App.tsx`, `main.tsx`).
- `server.ts`: Backend server implementation.
- `mail.db`: Local SQLite database.
- `.env`: Environment configuration (not tracked by Git).
- `metadata.json`: Additional project metadata.

## 🛠️ Setup & Installation

### Prerequisites
- **Node.js** (Latest LTS recommended)
- **npm**

### Installation Steps
npm install

npm run dev   
1. **Clone the repository:**
   ```bash
   git clone [https://github.com/akashsingh7432/Inbox.git](https://github.com/akashsingh7432/Inbox.git)
   cd personal-mail